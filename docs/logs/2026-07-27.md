# Log de Programação — 2026-07-27

## 16:20 — [FEAT] Tests atualizados para novo schema (ALTERACOES)

**Fase:** 6.7
**Arquivos alterados:**
- `database/factories/StageFactory.php` — proof_id → competition_id, stage_type adicionado
- `database/factories/BonusOnusFactory.php` — **CRIADO** (antes não existia)
- `database/factories/ProofFactory.php` — **REMOVIDO**
- `database/factories/FinalEnigmaFactory.php` — **REMOVIDO**
- `database/factories/FinalEnigmaQrCodeFactory.php` — **REMOVIDO**
- `database/seeders/CompetitionSeeder.php` — reescrito sem Proof/FinalEnigma, 4 stages + enigma_final, 2 equipes
- `database/migrations/2026_07_27_190731_complete_restructure.php` — fix SQLite (drop unique/index antes de dropColumn), stage_type/sub_questions/compass/unlock/word/cooldown columns adicionados
- `app/Models/BonusOnus.php` — adicionado `$table = 'bonus_onus'`
- `app/Models/TeamBonusOnus.php` — adicionado `$table = 'team_bonus_onus'`
- `tests/Unit/GameEngineTest.php` — reescrito: 21 testes, sem Proof/FinalEnigma references, valida QR+GPs, unlock, answer, scan bonus/onus, word guess, buy hint, first team bonus
- `tests/Feature/Api/StageApiTest.php` — reescrito: 9 testes, routes corrigidas (uuid em scan, validate-qr + answer + unlock)
- `CHECKLIST.md` — 6.7 tests atualizados marcado concluído

**Ação:** Modificado / Criado / Removido
**Detalhes:**
- StageFactory atualizada com `competition_id` e `stage_type`
- BonusOnusFactory criada com estados `bonus()` e `onus()`
- ProofFactory/FinalEnigmaFactory removidos
- CompetitionSeeder reescrito: 4 stages normais + 1 enigma_final, 2 equipes, sem Proof/FinalEnigma
- Migration complete_restructure: dropUnique + dropIndex antes de dropColumn para compatibilidade SQLite; stage_type e outras colunas (sub_questions, compass, unlock, word, cooldown, etc) adicionadas
- BonusOnus/TeamBonusOnus models: `$table` explícito para evitar pluralização incorreta
- GameEngineTest: 21 testes para unlock, validateQrAndGps, validateAnswer, completeStage (first team bonus), scanBonusOnus, validateWordGuess, buyHint
- StageApiTest: 9 testes cobrindo unlock, validate-qr, answer, scan bonus/onus, autenticação

**Validação:** `php artisan test` — 32/32 tests passing ✅

---

## 20:14 — [FEAT] Preferências do sistema (Settings)

**Fase:** 6.5
**Arquivos alterados:**
- `database/migrations/2026_07_27_201114_create_system_preferences_table.php` — **CRIADO:** tabela `system_preferences` (key-value com type, label, group), seed com 7 prefs iniciais (school_name, school_address, school_latitude, school_longitude, school_logo_path, telao_map_zoom, telao_refresh_seconds)
- `app/Models/SystemPreference.php` — **CRIADO:** Model com helpers `getValue(key, default)`, `setValue(key, value, type?)` e `getAllGrouped()`, type casting automático (integer, float, boolean, string)
- `app/Livewire/Admin/Settings.php` — **REESCRITO:** componente Livewire com formulário completo, validação, upload de logo, `WithFileUploads`, `removeLogo()`
- `resources/views/livewire/admin/settings.blade.php` — **REESCRITO:** formulário completo com seções Escola (nome, endereço, lat/lng, logo) e Telão (zoom, refresh), validação inline, feedback de sucesso
- `app/Livewire/Telao.php` — **MODIFICADO:** `schoolLocation()` agora lê de `SystemPreference` em vez de `$competition->school_*`, import adicionado

**Ação:** Criado / Modificado
**Detalhes:** Sistema de preferências implementado via tabela key-value flexível. School info movido de competition (que permanece com as colunas para fallback) para preferências globais. Settings page com formulário completo, upload de logo, validação.

**Validação:** `php artisan test` — 32/32 tests passing ✅

---

## 21:10 — [FIX] Telão: pin da escola não aparecia + botão pin centraliza mapa

**Fase:** 3.2
**Arquivos alterados:**
- `app/Livewire/Telao.php` — `schoolLocation()` fallback para `competition.school_*` se system prefs vazias
- `resources/views/livewire/telao.blade.php` — escola passada no `x-data` (síncrono), `loadSchoolMarker()` lê de `this.schoolData`, `mapMarkers` dict keyed por team_id, `centerOnTeam(teamId)`, botão mapa no placar

**Ação:** Modificado
**Detalhes:** Pin da escola renderizado direto do Blade, sem async. Fallback competition. Botão "Mapa" centraliza no marker.

**Validação:** `php artisan test` — 32/32 tests passing ✅
